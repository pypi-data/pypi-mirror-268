[tool.poetry]
name = "simple_performance_timer"
version = "0.1.3"
description = ""
authors = ["w-okada"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
