[build-system]
requires = ["setuptools>=42", "pydantic>=1.9.0", "aiohttp>=3.7.4"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 90
target-version = ["py310", "py311", "py312"]
skip-magic-trailing-comma = true

[tool.isort]
profile = "black"
line_length = 90
