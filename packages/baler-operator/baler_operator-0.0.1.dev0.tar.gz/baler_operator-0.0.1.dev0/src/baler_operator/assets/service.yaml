---
apiVersion: v1
kind: Service
metadata:
  name: haystack-pipeline-service-{{ name }}
spec:
  selector:
    app: haystack-pipeline-{{ name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
