
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "superdupper-core"
version = "0.0.12"
description = "Superdupper core."
readme = "README.md"

[tool.setuptools.packages.find]
include = ["superdupper.core"]
