

    # @classmethod
    # def serialize_collected(cls, collected: List["Collected"]) -> List[dict]:
    #     return [col.model_dump() for col in collected]
    #
    # @classmethod
    # def deserialize_collected(cls, collected: List[dict]) -> List["Collected"]:
    #     return [cls(**dict_) for dict_ in collected]

