# from click.testing import CliRunner
# from audiospotter_cli.cli import run


def test_connection():
    # runner = CliRunner()
    # result = runner.invoke(run, ["connect"])
    # assert result.exit_code == 0
    # assert (
    #     result.output == "{'detail': 'Authentication credentials were not provided.'}\n"
    # )
    assert True == True
