[tool.poetry]
name = "crawler-analytics"
version = "0.1.13"
description = ""
authors = ["AprilNEA <github@sku.moe>"]
readme = "README.md"
packages = [{ include = "crawler_analytics" }]

[tool.poetry.dependencies]
python = "^3.8"
redis = "^5.0.3"
nanoid = "^2.0.0"
aiohttp = "^3.9.3"


[tool.poetry.scripts]
crawler-analytics-server = "crawler_analytics.server:server"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

#[[tool.poetry.source]]
#name = "mewtant"
#url = "https://repo.mori.mewtant.co/repository/mewtant-pypi/simple"
#priority = "explicit"

