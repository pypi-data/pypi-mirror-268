[tool.poetry]
name = "exp-orchestrator"
version = "0.1.0"
description = "automatically manage and launch complex experiments from slurm (focused on ML workflows)"
authors = ["nightingal3"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
numpy = "^1.26.4"
pandas = "^2.2.2"
pytest = "^8.1.1"
pyyaml = "^6.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
