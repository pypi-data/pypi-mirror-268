[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "exp_hurst"
version = "0.0.2"
authors = [
  { name="Igor Jasenovski", email="igormjsk@gmail.com" },
]
description = "It computes the Hurst exponent of a time series."
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
