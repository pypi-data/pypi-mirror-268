[framework]
default_env = DEMO_qa
# Supports: empty (default), DEMO_qa  # Handled during run
default_browser = chrome
# Supports: chrome (default), firefox, edge  # Handled during run
project_id = 
# Unique short name to identify the project dir

is_use_flaky =
# Supports: empty (default), true

is_use_thread_data_file = true
# Supports: true (default), empty
;is_clear_thread_data_after_use = true
# Supports: true (default), empty
is_skip_on_data_found =
# Supports: empty (default), true
is_skip_on_data_prep =
# Supports: empty (default), true

[ui]
pageload_waittime_in_sec = 10
element_waittime_in_sec = 20

[ssh]
ssh_service_provider = xtermjs
# Supports: xtermjs (default), paramiko
is_run_yarn_for_xtermjs =
# Supports: empty (default), true
;xtermjs_code_path = resources/xtermjs_code
xtermjs_code_path = D://xtermjs_code
xtermjs_terminal_url = http://127.0.0.1:3000
xtermjs_logfile = log_xterm.log

[db]
update_max_waittime_in_sec = 60
oracle_instaclient_dirpath = D://Oracle/instant_client

[jira]
is_update_jira =
# Supports: empty (default), true
jira_server = https://company.atlassian.net
jira_username = user@email.com
jira_token = Jira API Token
zephyr_url = https://api.zephyrscale.smartbear.com/v2/
zephyr_token =
zephyr_test_execution_endpoint = testexecutions
zephyr_project_key =
zephyr_cycle_key =

[mobile]

