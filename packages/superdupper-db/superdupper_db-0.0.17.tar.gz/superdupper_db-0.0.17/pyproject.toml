
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "superdupper-db"
version = "0.0.17"
description = "Superdupper db."
readme = "README.md"

[tool.setuptools.packages.find]
include = ["superdupper.db"]
