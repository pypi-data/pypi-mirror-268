[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "proxybandit"
version = "1.0.0"
dependencies = [
    "aiohttp",
    "beautifulsoup4",
    "lxml"
]