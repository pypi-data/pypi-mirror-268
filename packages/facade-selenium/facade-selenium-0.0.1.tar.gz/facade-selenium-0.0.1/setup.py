from setuptools import setup, find_packages

setup(
    name="facade-selenium",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[
        # dependencies selenium>=4.0.1
    ]
)